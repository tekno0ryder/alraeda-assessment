import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alraeda-assessment',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
